리눅스 명령어
- ls
- ls 옵션a
- touch
- cd 폴더명
- cd ..
- cat 파일명

- echo "출력할문자열" >> 파일명
- rm 파일명
- history

깃 명령어
- git init
- git status
- git add 파일명
- git add .
- git config --global user.email "개인이메일주소"
- git config --global user.name "ID"
- git config --list
- git commit -m "커밋에 남길 메세지"
- git log
- git checkout 해시코드전체 또는 7자리
- git checkout -

- git branch -M main
- git remote add origin https://github.com/아이디/원격리포지토리.git
- git push -u origin main
- git push
- git remote -v
- git remote remove origin
- git clone 원격리포지토리주소
- git clone 원격리포지토리주소 .
-